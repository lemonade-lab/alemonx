# 前端开发规范

## UI 与样式

- 前端 UI 使用 Tailwind CSS、SCSS 和 `classnames` 实现，优先使用 Tailwind CSS 工具类，尽可能减少自定义 class。
- UI 必须遵循 Tailwind CSS 已定义的设计标准，包括间距、颜色、字体、圆角、阴影和响应式断点；不要随意定义与其不一致的样式值。
- 尽可能避免使用 Ant Design（antd）组件。确有必要时，应先确认本地是否已有可复用组件。
- 遇到通用功能或通用交互时，必须复用项目本地已定义的组件，不要重复自行实现。
- 新组件必须同时考虑明暗主题，不能只适配单一主题。
- 所有界面必须具备响应式表现，并优先保证小窗口和窄屏下的可用性；不得依赖固定屏幕尺寸。
- 设计状态必须完整：至少考虑默认、加载、空数据、错误、禁用、悬停、聚焦和提交中状态，避免只实现成功态。
- 图标只表达含义时必须提供可访问名称（如 `aria-label` 或可见文本）；不要用颜色作为传达状态或错误的唯一方式。
- 表单控件必须关联 `label`，支持键盘操作并保留清晰的焦点样式；弹窗、抽屉等浮层应支持 Esc 关闭和合理的焦点管理。
- 所有用户可见文案统一使用中文，避免散落的硬编码文案；重复文案、状态文本和格式化逻辑应集中管理，便于后续国际化。

## 组件与代码组织

- 按窗口或功能边界拆分组件，禁止将所有组件堆放在同一个文件中。
- 组件的类型声明应方便独立维护：例如 `A.tsx` 的类型优先放在同级 `A.d.ts` 文件中。
- 新组件应尽可能采用懒加载；仅在不影响首屏体验、交互连续性和代码可读性的场景下使用。
- 单个新组件文件不得超过 1200 行；接近该上限时必须继续拆分职责。
- 组件应保持单一职责：页面负责组合与编排，业务组件负责具体功能，基础组件负责通用展示与交互；不要在展示组件中混入复杂请求、状态编排或数据转换。
- 优先使用组合（`children`、插槽式 props）复用 UI；避免通过不断增加布尔 props 形成难以维护的“万能组件”。
- props、事件和数据结构必须有明确类型；避免新增 `any`、隐式 `any`、宽泛的 `object` 或 `Function` 类型。类型能从接口或数据模型推导时，优先推导而非重复声明。
- 组件对外 API 应稳定、语义清晰。新增或修改公共组件时，必须考虑已有调用方的兼容性。
- 仅在业务意图不明显、存在边界条件或临时取舍时写注释；注释应解释“为什么”，不复述代码“做什么”。

## 状态与数据

- 所有前端状态统一使用 Redux 管理，并配置持久化。
- Redux state 必须保持可序列化；禁止保存函数、React 节点、DOM 对象、Promise、`Map`、`Set` 等不可序列化对象。
- 持久化 state 必须设置版本与迁移策略，并只持久化确有跨刷新价值的数据；不得持久化密码、令牌、会话凭证或其他敏感信息。
- 异步数据请求必须统一通过项目既有的请求封装或数据访问层；处理加载、成功、失败、取消和竞态，避免组件各自实现不一致的请求逻辑。
- 服务端数据与本地 UI 状态应分层管理；不要把可重新获取的服务端响应无差别地长期持久化。
- 对用户输入、URL 参数和服务端返回的展示内容保持不信任。渲染富文本或 HTML 前必须进行净化，避免 XSS；不得在前端代码、日志或 Redux 持久化中暴露敏感信息。

## 状态与性能

- 新增功能必须评估性能影响，包括渲染次数、包体积、请求数量、内存占用和首屏加载；避免不必要的重复计算、重复请求和大体积同步加载。
- 不得为了“优化”而滥用 `memo`、`useMemo`、`useCallback`；应先通过 React DevTools、浏览器 Performance 面板或构建产物分析确认瓶颈。
- 大列表、日志流和高频更新区域必须考虑虚拟化、分页、增量渲染、节流或防抖，并在组件卸载时清理定时器、订阅和事件监听。
- 图片、图标和静态资源应按需加载并选择合适尺寸；避免引入体积较大的依赖来解决简单问题。新增依赖前先检查现有依赖与本地组件是否已能满足需求。
- 懒加载边界必须提供可用的加载态和失败兜底，不能造成空白页面或未捕获异常。

## 跨平台路径与受管进程

- 机器人目录是跨前后端、代理 URL、运行状态、PM2 与插件 WebView 的身份键。任何新增入口都必须接受 Windows 盘符路径（如 `C:\\bots\\demo`）和 UNC 路径，不能假设绝对路径总以 `/` 开头。
- 使用目录令牌的路由必须复用 `decodeRobotRootToken`；浏览器端令牌必须以 UTF-8 base64url 编码，服务端解码后仍要验证为绝对路径，再交给机器人管理层校验。
- 不要用裸字符串比较文件系统路径。先清理路径；凡是可能来自 Windows 文件系统、PM2、Git 或其他外部工具的路径比较，必须按 Windows 的大小写不敏感语义处理，并保留 Unix 的大小写敏感语义。
- Windows 无法可靠读取监听进程的工作目录。端口归属判断应以受管父进程/进程树识别 npm、yarn 等启动器派生的 Node 进程，不能把“无法读取 CWD”直接判为其他机器人。
- 新增端口、反向代理、WebView、PM2 或进程管理功能时，至少覆盖：Windows 盘符路径令牌、UNC 路径（如适用）、路径大小写差异、npm/yarn 子进程占用端口和非当前机器人端口冲突。Go 测试应在 Windows 上运行；非 Windows CI 至少交叉编译 `GOOS=windows GOARCH=amd64 go test -c`。
- 机器人终端以机器人目录或其子目录作为工作目录，`cd` 状态必须拒绝父目录和越界符号链接；但允许任意 shell 命令、管道和重定向。工作目录不是文件系统沙箱，命令显式引用 `..` 或绝对路径仍受本机账户权限控制；若产品需要强隔离，必须另行设计 OS/容器级沙箱，不能靠命令白名单冒充终端。
- 所有 Node.js 环境检查必须复用 `system.Checker` 的最低版本 `v22.22.3`。低于该版本应标记为 `outdated` 并显示升级入口，但不得阻止用户继续；只有缺失或无法运行的环境才是阻断项。不要在单个前端页面重复实现版本比较。
- “浏览器及依赖包”是 Puppeteer 等浏览器自动化的可选环境。检查项必须标记为 optional，不能阻断流程；在 RPM/DNF/YUM 主机上，安装方案必须包含固定的 Chrome/Chromium 运行库与字体清单，且不得执行来自浏览器的任意系统命令。

## 质量与测试

- 每次改动完成后至少执行与改动范围匹配的检查：`yarn lint`、`yarn build`；涉及关键交互、路由、SSE 或端到端流程时，补充或更新 Playwright 用例并执行 `yarn test:sse`。
- 新功能和修复应覆盖核心用户路径、边界条件与失败路径；测试应验证用户可见行为，不依赖实现细节或脆弱的 DOM 结构。
- 修复缺陷时，应在可行情况下先补充能复现问题的测试，再修复实现，防止回归。
- 不要为通过检查而关闭 ESLint、TypeScript 或测试规则；确需例外时，缩小范围并说明原因。
- 提交前清理调试代码、无用依赖、无用导入、死代码、`console.log` 和临时 mock；保留的日志必须有明确的诊断价值且不得包含敏感数据。

## 错误处理与可观测性

- 页面和异步功能必须提供用户可理解、可恢复的错误反馈；技术细节可记录到日志，但不应直接暴露给用户。
- 关键页面或功能区应设置错误边界或等效兜底，避免单个组件异常导致整页不可用。
- 可重试的操作提供明确的重试入口；重试需避免无限循环、重复提交和请求风暴。
- 记录错误与关键操作时使用结构化、可检索的信息，并注意脱敏；不得记录密码、令牌、个人敏感信息或完整业务内容。

## 变更边界与交付

- 修改前先阅读相关组件、Redux slice、公共组件和已有实现；以最小改动满足需求，不顺手重构无关代码。
- 新增页面、窗口、状态或接口契约时，必须同步检查路由、权限、导航、加载态、错误态、暗色主题和持久化升级的影响。
- 破坏性变更必须明确迁移方案和兼容策略；未经确认不得修改或删除用户已有的持久化数据。
- 不得直接修改生成文件、构建产物或锁文件以掩盖问题；依赖变更必须说明必要性和体积、许可证及安全影响。
